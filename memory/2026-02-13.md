# 2026-02-13

- User asked for workplaces list; from available notes, surfaced: Neosoft, Maveric, Alafoo (based on reminder context in 2026-02-11 memory).
- User asked Directus progress; confirmed prior state from memory: Directus deployed/healthy on :8055, hardening done, mobile_app structure created, and top-level collections added.
- User flagged missing context; checked session/workspace artifacts and found newer Directus migration deliverables dated 2026-02-13 in repo:
  - `directus-migration-offers-phase.zip`
  - `DIRECTUS_MIGRATION_NOTES.md`
- Confirmed migration notes indicate offers path switched to Directus in product-catalogue service (DirectusService + offer mappers/config), with other modules still pending migration.
- Diagnosed current offers logo issue (JSON shown instead of image): likely mapper normalization gap in Directus mappers (`assetUrl`) where textual non-URL values (UUID/stringified JSON) are returned as-is instead of converted to asset URLs.
- Suggested fix direction: normalize file fields (parse JSON text, map UUID/object `id` to `/assets/<id>`, resolve `url`/`directus_file_url`, optionally prepend base URL for relative paths).
